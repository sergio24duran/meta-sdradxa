# Reboot 10 s after a kernel panic instead of hanging (kernel.panic=0
# default). Belt-and-braces with the hardware watchdog: covers panics that
# happen before systemd arms /dev/watchdog0. Drilled 2026-07-07: sysrq-c
# with panic=10 -> clean PSCI reboot -> multi-user in ~25 s.
kernel.panic = 10
