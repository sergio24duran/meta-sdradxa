diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index e7f9378..c287dd9 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -44,11 +44,9 @@ message(STATUS "CMAKE_FIND_ROOT_PATHS: ${CMAKE_FIND_ROOT_PATH}")
 if(NOT BUILD_WITHOUT_CURL)
     find_package(CURL REQUIRED)
 endif()
-if (CMAKE_CROSSCOMPILING)
-    find_package(LibLZMA HINTS "${CMAKE_PREFIX_PATH}" REQUIRED)
-else()
-    find_package(LibLZMA REQUIRED)
-endif()
+# HINTS forces CONFIG mode, which system liblzma (xz) does not provide; use
+# the built-in FindLibLZMA module in both cases.
+find_package(LibLZMA REQUIRED)
 
 if(BUILD_TESTING AND (IOS OR ANDROID))
     message(STATUS "Building for iOS or Android: forcing BUILD_TESTING to FALSE...")
